import dotenv from 'dotenv';
dotenv.config();

export const { SERVER_API_BASE } = process.env;

export const API_URL = 'https://server.uparupa.town:9090';
// export const API_URL = 'http://127.0.0.1:8080';